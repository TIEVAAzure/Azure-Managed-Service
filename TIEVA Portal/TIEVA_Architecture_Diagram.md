# TIEVA Portal - Complete Architecture & Communication Flow

## Component Overview

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                                    USERS                                                 │
│                          (Browser / Azure AD Auth)                                       │
└───────────────────────────────────────┬─────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│  ① AZURE STATIC WEB APP                                                                 │
│  https://ambitious-wave-092ef1703.3.azurestaticapps.net                                 │
│  ─────────────────────────────────────────────────────────────────────────────────────  │
│  • Hosts: portal/index.html (frontend)                                                  │
│  • Auth: Azure AD via staticwebapp.config.json                                          │
│  • Route /api/* requires "authenticated" role                                           │
│  • Has "Linked Backend" configured to func-tievaportal-6612                             │
│  ─────────────────────────────────────────────────────────────────────────────────────  │
│  📋 staticwebapp.config.json:                                                           │
│     - "/*" → requires "authenticated"                                                   │
│     - "/api/*" → requires "authenticated"                                               │
│     - 401 → redirects to /.auth/login/aad                                               │
└───────────────────────────────────────┬─────────────────────────────────────────────────┘
                                        │
                    ┌───────────────────┴───────────────────┐
                    │                                       │
                    ▼ (Direct - Browser)                    ▼ (SWA Linked Backend)
┌───────────────────────────────────────┐   ┌───────────────────────────────────────────┐
│  CURRENT: Direct API Calls            │   │  ALTERNATIVE: Via SWA Proxy               │
│  ─────────────────────────────────    │   │  ─────────────────────────────────────    │
│  Frontend JS uses:                    │   │  Frontend would use:                      │
│  const API = 'https://func-tieva...'  │   │  const API = '/api'                       │
│  + API_KEY query param                │   │  (No API key needed - SWA injects auth)   │
│                                       │   │                                           │
│  ⚠️  BYPASSES SWA AUTH!               │   │  ✅ Would inherit SWA auth                │
└───────────────────────────┬───────────┘   └───────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│  ② MAIN API - FUNCTION APP (.NET 8 Isolated)                                            │
│  https://func-tievaportal-6612.azurewebsites.net/api                                    │
│  ─────────────────────────────────────────────────────────────────────────────────────  │
│  Auth Level: ANONYMOUS (⚠️ VULNERABILITY)                                               │
│  All functions use: AuthorizationLevel.Anonymous                                        │
│  ─────────────────────────────────────────────────────────────────────────────────────  │
│  KEY FILES:                                                                             │
│  ├── Functions/CustomerFunctions.cs     → /api/customers/*                              │
│  ├── Functions/AssessmentFunctions.cs   → /api/assessments/*                            │
│  ├── Functions/ConnectionFunctions.cs   → /api/connections/*                            │
│  ├── Functions/AuditProxyFunctions.cs   → /api/assessments/start (PROXY)                │
│  ├── Functions/FinOpsFunctions.cs       → /api/finops/*                                 │
│  ├── Functions/TierFunctions.cs         → /api/tiers/*                                  │
│  └── Functions/SettingsFunctions.cs     → /api/settings/*                               │
│  ─────────────────────────────────────────────────────────────────────────────────────  │
│  PROXY PATTERN (AuditProxyFunctions.cs):                                                │
│  • POST /api/assessments/start → Forwards to func-tieva-audit                           │
│  • POST /api/finops/setup → Forwards to func-tieva-audit                                │
│  • Uses: AUDIT_API_URL + AUDIT_API_KEY environment variables                            │
│  ─────────────────────────────────────────────────────────────────────────────────────  │
│  BACKEND CONNECTIONS:                                                                   │
│  • SQL Database: Azure SQL (connection string in app settings)                          │
│  • Blob Storage: sttievaaudit (via Managed Identity)                                    │
│  • Key Vault: kv-tievaPortal-874 (via Managed Identity)                                 │
└───────────────────────────────────────┬─────────────────────────────────────────────────┘
                                        │
                                        │ (HTTP POST with API Key)
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│  ③ AUDIT API - FUNCTION APP (PowerShell)                                                │
│  https://func-tieva-audit.azurewebsites.net/api                                         │
│  ─────────────────────────────────────────────────────────────────────────────────────  │
│  Auth Level: ANONYMOUS (⚠️ VULNERABILITY)                                               │
│  function.json: "authLevel": "anonymous"                                                │
│  ─────────────────────────────────────────────────────────────────────────────────────  │
│  KEY FUNCTIONS:                                                                         │
│  ├── StartAssessment/run.ps1  → POST /api/assessments/start                             │
│  │   • Receives: connectionId, module                                                   │
│  │   • Connects to customer tenant via Service Principal                                │
│  │   • Runs audit script (NetworkAudit.ps1, etc.)                                       │
│  │   • Uploads results to Blob Storage                                                  │
│  │   • 🔄 CALLS BACK to Main API to save results!                                       │
│  │                                                                                      │
│  └── SetupFinOps/run.ps1  → POST /api/finops/setup                                      │
│  ─────────────────────────────────────────────────────────────────────────────────────  │
│  CRITICAL CALLBACKS (run.ps1 lines ~130-180):                                           │
│  • POST $apiBase/assessments                    → Create assessment record              │
│  • PUT  $apiBase/assessments/{id}               → Update assessment status              │
│  • POST $apiBase/assessments/{id}/modules       → Add module result                     │
│  • POST $apiBase/assessments/{id}/modules/{m}/parse → Auto-parse findings               │
│  ─────────────────────────────────────────────────────────────────────────────────────  │
│  Environment Variables (in Azure):                                                      │
│  • TIEVA_API_URL = "https://func-tievaportal-6612.azurewebsites.net/api"                │
│  • TIEVA_API_KEY = (function key for main API)                                          │
└─────────────────────────────────────────────────────────────────────────────────────────┘

## Data Flow: Running an Assessment

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│  STEP-BY-STEP ASSESSMENT FLOW                                                           │
│═════════════════════════════════════════════════════════════════════════════════════════│
│                                                                                         │
│  1. User clicks "Run Assessment" in Portal                                              │
│     └─→ Browser JS: fetch(API + '/assessments/start', {connectionId, module})           │
│                                                                                         │
│  2. Main API receives request (AuditProxyFunctions.cs)                                  │
│     └─→ Proxies to: func-tieva-audit/api/assessments/start                              │
│                                                                                         │
│  3. Audit Function (StartAssessment/run.ps1) executes:                                  │
│     a. Calls Main API: GET /connections/{id}/audit-subscriptions/{module}               │
│     b. Retrieves Service Principal secret from Key Vault                                │
│     c. Connects to customer Azure tenant                                                │
│     d. Runs audit script (e.g., NetworkAudit.ps1)                                       │
│     e. Generates Excel file with Findings worksheet                                     │
│     f. Uploads Excel to Blob Storage (sttievaaudit/audit-results/)                      │
│     g. 🔄 Calls BACK to Main API:                                                       │
│        • POST /assessments (create record)                                              │
│        • PUT  /assessments/{id} (update status)                                         │
│        • POST /assessments/{id}/modules (add module result)                             │
│        • POST /assessments/{id}/modules/{m}/parse (parse findings into DB)              │
│                                                                                         │
│  4. Main API stores everything in SQL Database                                          │
│                                                                                         │
│  5. Response flows back to browser                                                      │
│                                                                                         │
└─────────────────────────────────────────────────────────────────────────────────────────┘

## Why SWA Linking Broke Callbacks

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│  THE PROBLEM WITH PREVIOUS SWA LINKING ATTEMPT                                          │
│═════════════════════════════════════════════════════════════════════════════════════════│
│                                                                                         │
│  When SWA was linked AND frontend used '/api' (proxied):                                │
│                                                                                         │
│  1. Browser → SWA → Main API (OK - SWA injects auth headers)                            │
│                                                                                         │
│  2. Main API → Audit API (OK - uses API key)                                            │
│                                                                                         │
│  3. Audit API → Main API CALLBACK (💥 FAILED!)                                          │
│     │                                                                                   │
│     └─→ Why? The callback URL in TIEVA_API_URL still pointed to:                        │
│         https://func-tievaportal-6612.azurewebsites.net/api                             │
│                                                                                         │
│         If we enabled Azure AD auth on the Function App itself,                         │
│         the Audit Function's callbacks would fail because:                              │
│         • PowerShell callbacks don't have a user session                                │
│         • They can't authenticate as an Azure AD user                                   │
│         • They only have the function key (TIEVA_API_KEY)                               │
│                                                                                         │
│  TWO AUTHENTICATION MODELS IN CONFLICT:                                                 │
│  ┌─────────────────────────┐    ┌─────────────────────────────────┐                     │
│  │  SWA/Browser Traffic    │    │  Server-to-Server (Callbacks)   │                     │
│  │  ───────────────────    │    │  ─────────────────────────────  │                     │
│  │  • User is logged in    │    │  • No user context              │                     │
│  │  • Has Azure AD token   │    │  • Service-to-service call      │                     │
│  │  • SWA validates        │    │  • Uses function key            │                     │
│  └─────────────────────────┘    └─────────────────────────────────┘                     │
│                                                                                         │
└─────────────────────────────────────────────────────────────────────────────────────────┘

## Current Security Gaps

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│  CURRENT VULNERABILITIES                                                                │
│═════════════════════════════════════════════════════════════════════════════════════════│
│                                                                                         │
│  1. Main API (func-tievaportal-6612) - ANONYMOUS ACCESS                                 │
│     ────────────────────────────────────────────────────                                │
│     • Anyone can call any endpoint directly                                             │
│     • Customer data exposed: GET /api/customers                                         │
│     • Assessment data exposed: GET /api/assessments                                     │
│     • Can create/delete data: POST/DELETE endpoints                                     │
│                                                                                         │
│     Current "protection": Function key in query string                                  │
│     Problem: Key is in frontend JavaScript - publicly visible!                          │
│                                                                                         │
│  2. Audit API (func-tieva-audit) - ANONYMOUS ACCESS                                     │
│     ─────────────────────────────────────────────────                                   │
│     • Anyone can trigger assessments if they know connection IDs                        │
│     • Could consume Azure resources maliciously                                         │
│     • Less critical (needs valid connectionId) but still a risk                         │
│                                                                                         │
└─────────────────────────────────────────────────────────────────────────────────────────┘

## Recommended Solution

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│  RECOMMENDED APPROACH: HYBRID AUTHENTICATION                                            │
│═════════════════════════════════════════════════════════════════════════════════════════│
│                                                                                         │
│  OPTION 1: Custom Header Validation (Simplest - No Azure Portal Changes)                │
│  ───────────────────────────────────────────────────────────────────────                │
│                                                                                         │
│  1. Frontend changes:                                                                   │
│     - Change: const API = '/api'  (use SWA proxy)                                       │
│     - Remove: const API_KEY = '...'                                                     │
│     - All calls go through SWA (gets X-MS-CLIENT-PRINCIPAL header)                      │
│                                                                                         │
│  2. Main API changes:                                                                   │
│     - Add middleware to check for EITHER:                                               │
│       a. Valid X-MS-CLIENT-PRINCIPAL header (SWA traffic), OR                           │
│       b. Valid TIEVA_API_KEY in query/header (server-to-server callbacks)               │
│                                                                                         │
│  3. Keep Audit API as-is (only Main API calls it with API key)                          │
│                                                                                         │
│  OPTION 2: Enable Azure AD on Function App + Managed Identity (More Secure)             │
│  ──────────────────────────────────────────────────────────────────────────             │
│                                                                                         │
│  1. Enable Azure AD auth on func-tievaportal-6612                                       │
│  2. Give func-tieva-audit a Managed Identity                                            │
│  3. Grant that MI permission to call the Main API                                       │
│  4. Update PowerShell to get Azure AD token for callbacks                               │
│                                                                                         │
│  More complex but fully AAD-protected                                                   │
│                                                                                         │
└─────────────────────────────────────────────────────────────────────────────────────────┘

## File Reference

```
TIEVA Portal/
├── portal/
│   ├── index.html                    # Frontend (Line 2172: API config)
│   └── staticwebapp.config.json      # SWA routing/auth rules
│
├── functions/
│   ├── TIEVA.Functions/              # Main API (.NET 8)
│   │   └── Functions/
│   │       ├── AuditProxyFunctions.cs   # Proxies to Audit API
│   │       ├── AssessmentFunctions.cs   # Assessment CRUD
│   │       ├── CustomerFunctions.cs     # Customer CRUD
│   │       ├── ConnectionFunctions.cs   # Azure connections
│   │       └── ...
│   │
│   └── TIEVA.Audit/                  # Audit API (PowerShell)
│       ├── StartAssessment/
│       │   ├── function.json         # Route config
│       │   └── run.ps1               # Assessment logic + CALLBACKS
│       ├── SetupFinOps/
│       │   └── run.ps1               # FinOps setup
│       └── Scripts/
│           ├── NetworkAudit.ps1
│           ├── BackupAudit.ps1
│           └── ...
```
